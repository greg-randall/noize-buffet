# Copy this file to .env and fill in your keys. .env is never committed.

# TypeSafe API key (used by comment mining). start.py stops without it unless you run it with --no-typesafe.
# Get one at https://typesafe.ai
TYPESAFE_API=
